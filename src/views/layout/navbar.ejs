<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <% navigation.map(elem => {%>
      <li class="nav-item">
        <a class="nav-link" href="<%= elem.link %>"><%= elem.name %></a>
      </li>
      <% }) %>
    </ul>
  </div>
</nav>